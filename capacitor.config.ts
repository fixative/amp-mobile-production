import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vss-production',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
