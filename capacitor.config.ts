import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ntonic.vssamp',
  appName: 'VSS-AMP',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
